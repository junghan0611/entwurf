# entwurf

pi-package — 텔레그램 브릿지 (DM ↔ pi 세션)

## 설치

```bash
pi install /path/to/entwurf
```

## 빌드

```bash
npm install
npm run build
```

## 사용

pi 실행 시 `--telegram` 플래그로 활성화. 환경변수 `PI_TELEGRAM_BOT_TOKEN`, `PI_TELEGRAM_CHAT_ID` 필요.
