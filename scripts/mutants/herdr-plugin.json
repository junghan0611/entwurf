{
	"schemaVersion": 1,
	"lane": "herdr-plugin",
	"mutants": [
		{
			"claim": "HPL-ONE-OPEN-ONE-READ",
			"title": "re-reading either listing turns one answered question into a poll — peer-facts observes every citizen unbounded (measured 4.1s on a 1,150-record store) and herdr publishes no 'the session reference landed' event, so a second read is both the slow path and the discovery watcher docs/mux-launch-rail.md §7 refuses by name (#116 M2-b)",
			"subject": "plugins/herdr/lib/status.mjs",
			"find": ["\tconst agents = readJson(herdrBin, [\"agent\", \"list\"], {"],
			"replace": [
				"\treadJson(herdrBin, [\"agent\", \"list\"], { failed: \"x\", unparsable: \"y\" });\n\tconst agents = readJson(herdrBin, [\"agent\", \"list\"], {"
			],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-ONE-OPEN-ONE-READ]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-SKIP-NOT-FAILURE",
			"title": "turning a missing Entwurf into a red makes every host that simply has not installed it look broken, and invites the plugin to 'fix' it — installing Entwurf is not a plugin's business (AGENTS.md Hard Rule 17, #116 M2-b)",
			"subject": "plugins/herdr/lib/status.mjs",
			"find": ["\t\t\t\t\tcode: \"entwurf-not-found\","],
			"replace": ["\t\t\t\t\tcode: \"quietly-nothing\","],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-SKIP-NOT-FAILURE]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-FAILED-READ-IS-RED",
			"title": "treating a nonzero listing exit as success renders an empty table for a listing that was never read — a host whose store could not be opened becomes indistinguishable from a host with no citizens (#116 M2-b) (#116 P1: the non-zero check moved into the single `spawnOnce` call site, so the mutation follows its subject — both listings now lose their exit check at once, which is the same defect stated in one place)",
			"subject": "plugins/herdr/lib/status.mjs",
			"find": [
				"\tif (run.status !== 0) return { error: failedCode, detail: `exit ${run.status}: ${(run.stderr ?? \"\").trim()}` };"
			],
			"replace": ["\tif (false) return { error: failedCode, detail: `exit ${run.status}` };"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-FAILED-READ-IS-RED]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-AMBIGUOUS-NOT-GUESSED",
			"title": "resolving two agent rows on one pane to whichever came first is a guess wearing a value's clothes — the only worth of this column is that it never guesses (#116 M2-b)",
			"subject": "plugins/herdr/lib/status.mjs",
			"find": ["\t\tbyPane.set(paneId, byPane.has(paneId) ? \"ambiguous\" : status);"],
			"replace": ["\t\tif (!byPane.has(paneId)) byPane.set(paneId, status);"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-AMBIGUOUS-NOT-GUESSED]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-ACTIVITY-IS-SEPARATE",
			"title": "labelling herdr's agent_status as anything but herdr's own report starts the drift that ends with a screen judgement being read as Entwurf delivery liveness (docs/herdr-launch-rail.md §9, #116 M2-b)",
			"subject": "plugins/herdr/lib/status.mjs",
			"find": ["\tconst head = [\"garden id\", \"backend\", \"cwd\", \"placement\", \"herdr-reported activity\"];"],
			"replace": ["\tconst head = [\"garden id\", \"backend\", \"cwd\", \"placement\", \"liveness\"];"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-ACTIVITY-IS-SEPARATE]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-DIAGNOSTICS-SHOWN",
			"title": "dropping the provider's diagnostics shows a store carrying hazards as a clean listing — the one lie a status pane can tell while looking green (#116 M2-b)",
			"subject": "plugins/herdr/lib/status.mjs",
			"find": [
				"\t\tlines: [...renderTable(peers, indexActivityByPane(agentRows)), \"\", ...renderDiagnostics(diagnostics)],"
			],
			"replace": [
				"\t\tlines: [...renderTable(peers, indexActivityByPane(agentRows)), \"\", ...renderDiagnostics([])],"
			],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-DIAGNOSTICS-SHOWN]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-JOIN-PANE-ID-ONLY",
			"title": "joining on a native session id forks the vendor floor Entwurf owns in pi-extensions/lib/herdr-placement.ts into a file no gate and no document covers — the whole join a plugin may make is one opaque pane-id string equality (#116 M2-b)",
			"subject": "plugins/herdr/lib/status.mjs",
			"find": ["\t\tconst paneId = kind === \"herdr-pane\" ? placement.paneId : null;"],
			"replace": ["\t\tconst paneId = typeof peer?.nativeSessionId === \"string\" ? peer.nativeSessionId : null;"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-JOIN-PANE-ID-ONLY]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-NO-WRITE",
			"title": "keeping state in HERDR_PLUGIN_STATE_DIR gives a status pane something that can be stale — this one is only ever as old as the keypress that opened it, and that is the whole guarantee (#116 M2-b)",
			"subject": "plugins/herdr/lib/status.mjs",
			"find": ["\tholdOpen().then(() => process.exit(code));"],
			"replace": [
				"\tif (process.env.HERDR_PLUGIN_STATE_DIR) fs.writeFileSync(path.join(process.env.HERDR_PLUGIN_STATE_DIR, \"last.json\"), lines.join(\"\\n\"));\n\tholdOpen().then(() => process.exit(code));"
			],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-NO-WRITE]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-MANIFEST-NO-FORBIDDEN-SECTIONS",
			"title": "[[build]] became this plugin's ONE door in #116 M3-b3 and left the forbidden list, so the defect this claim must still catch is the volunteering kind: a [[startup]] hook runs on every herdr server start, in the operator's real HOME, without anyone asking — which is exactly the watcher-shaped growth this lane refuses (#116 M2-b, M3-b3)",
			"subject": "plugins/herdr/herdr-plugin.toml",
			"find": ["[[panes]]"],
			"replace": ["[[startup]]\ncommand = [\"node\", \"lib/status.mjs\"]\n\n[[panes]]"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-MANIFEST-NO-FORBIDDEN-SECTIONS]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-RENDER-IS-THE-VISIBLE-ONES",
			"title": "drawing every retained citizen turns the status pane into the whole store — measured 2026-09-15 the operator's own store is 1,162 records, all unobserved, 17,437 lines of peer-facts output, so finding the two citizens actually in this Herdr session becomes a scrolling exercise and a status surface you must search has already failed (#116 M2-b)",
			"subject": "plugins/herdr/lib/status.mjs",
			"find": ["\t\tif (kind !== \"herdr-pane\" && kind !== \"ambiguous\") {"],
			"replace": ["\t\tif (false) {"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-RENDER-IS-THE-VISIBLE-ONES]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-DIAGNOSTIC-SUBJECTS",
			"title": "printing a diagnostic's kind and prose without its subject tells an operator that a hazard exists and not where — `record-less-socket` without its garden id and `meta-record-read-error` without its filename are lines nobody can act on (#116 M2-b)",
			"subject": "plugins/herdr/lib/status.mjs",
			"find": ["\t\t\tconst fields = Object.keys(d ?? {})"],
			"replace": ["\t\t\tconst fields = [] || Object.keys(d ?? {})"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-DIAGNOSTIC-SUBJECTS]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-BLOCK-PROVENANCE",
			"title": "the activation block stops saying it is an install-time receipt and claims it was read now — the one sentence that keeps an add-only ledger from being mistaken for a live probe, and the moment it goes the four blocks collapse into one implied 'as of now'",
			"subject": "plugins/herdr/lib/capability-report.mjs",
			"find": ["\tactivation: \"install-time\","],
			"replace": ["\tactivation: \"now\","],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-BLOCK-PROVENANCE]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-NO-AGGREGATE-VERDICT",
			"title": "a summary line is added over the four blocks, which is exactly the merged green this surface exists to refuse: one word has to pick one observation time and be wrong about the other two",
			"subject": "plugins/herdr/lib/capability-report.mjs",
			"find": ["\tconst out = [];"],
			"replace": ["\tconst out = [\"Overall: all ready.\"];"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-NO-AGGREGATE-VERDICT]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-RAILS-SHARED-SOURCE",
			"title": "the RAILS block stops reading the shared declaration leaf and renders a hardcoded model string, re-opening the third-copy drift that leaf was created to close",
			"subject": "plugins/herdr/lib/capability-report.mjs",
			"find": [
				"\t\t\t`  ${backend}: ${railEntryNote[backend]}\\n    model string syntax, one example only: \\`${modelSyntaxExample[backend]}\\``,"
			],
			"replace": [
				"\t\t\t`  ${backend}: ${railEntryNote[backend]}\\n    model string syntax, one example only: \\`some-provider/some-model\\``,"
			],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-RAILS-SHARED-SOURCE]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-LEDGER-FALLBACK-READS-CITIZENS",
			"title": "the D3=C fallback is removed, so a correct plugin install — which puts nothing on PATH — closes its own first screen with entwurf-not-found again, and the operator is back to setting ENTWURF_BIN by hand",
			"subject": "plugins/herdr/lib/status.mjs",
			"find": ["\tif (evidence.kind === \"ledger\" && evidence.runtimeBin !== null) {"],
			"replace": ["\tif (false) {"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-LEDGER-FALLBACK-READS-CITIZENS]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-OVERRIDE-BEATS-LEDGER",
			"title": "the ledger is consulted BEFORE the operator's own ENTWURF_BIN and PATH, so a receipt silently out-votes the person at the keyboard — the failure mode a fallback is most likely to grow",
			"subject": "plugins/herdr/lib/status.mjs",
			"find": ["\tconst declared = process.env.ENTWURF_BIN;\n\tif (declared !== undefined && declared !== \"\") {"],
			"replace": [
				"\tconst declared = process.env.ENTWURF_BIN;\n\tif (evidence.kind !== \"ledger\" && declared !== undefined && declared !== \"\") {"
			],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-OVERRIDE-BEATS-LEDGER]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-ACTIVATION-NAMED-REDS",
			"title": "every broken activation state collapses into one generic code, so `we cannot import the reader`, `the body is not certifiable`, `this receipt is about another root` and `the promised binary is gone` become indistinguishable — four different repairs behind one word",
			"subject": "plugins/herdr/lib/activation-evidence.mjs",
			"find": ["\t\t\tcode: \"activation-runtime-root-mismatch\","],
			"replace": ["\t\t\tcode: \"activation-reader-unavailable\","],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-ACTIVATION-NAMED-REDS]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-LEDGER-AT-WRITER-PATH",
			"title": "the ledger is addressed at the RUNTIME's data root again instead of the writer's XDG state root, so on every correctly activated host the pane finds no ledger where one certainly exists — and because the writer's reader answers a missing file with `null`, the original of this mutation did not merely lose the D3=C fallback, it killed the pane before a single block was drawn",
			"subject": "plugins/herdr/lib/activation-evidence.mjs",
			"find": ["\t\tledgerPath: activation.ledgerPath,"],
			"replace": ["\t\tledgerPath: path.join(layout.pluginRoot, \"activation.json\"),"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-LEDGER-AT-WRITER-PATH]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-LEDGER-MISSING-IS-NAMED",
			"title": "a missing ledger is only noticed AFTER the installed reader has been located, so a host whose single real fact is that nothing ever wrote a receipt is told its runtime is broken instead — two different repairs behind one name, and the ledger's address needed no installed artifact to resolve in the first place",
			"subject": "plugins/herdr/lib/activation-evidence.mjs",
			"find": ["\tif (!ledgerPresent) {"],
			"replace": ["\tif (false) {"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-LEDGER-MISSING-IS-NAMED]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-ENV-ROOT-IS-NAMED",
			"title": "the layout resolvers are called outside any guard, so a host with neither HOME nor an XDG root takes the pane down on an unhandled rejection instead of being told, in the activation block, that we cannot even say where its ledger would live — the other three blocks never needed that root",
			"subject": "plugins/herdr/lib/activation-evidence.mjs",
			"find": [
				"\tlet roots;\n\ttry {\n\t\troots = deriveRoots(env, { runtimeModule, activationModule });\n\t} catch (err) {"
			],
			"replace": [
				"\tlet roots;\n\troots = deriveRoots(env, { runtimeModule, activationModule });\n\ttry {\n\t\tif (roots === undefined) throw new Error(\"unreachable\");\n\t} catch (err) {"
			],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-ENV-ROOT-IS-NAMED]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-PHASE-GATES-FALLBACK",
			"title": "any certified ledger grants the runtime fallback, including one written mid-teardown, so an add-only install-time receipt is spent as if it were a live probe — `deactivating` says what was wired, not what is standing there now, and merging those two is how this pane would start answering a runtime question with an install-time answer",
			"subject": "plugins/herdr/lib/activation-evidence.mjs",
			"find": ["\tif (ledger.phase !== \"active\") {"],
			"replace": ["\tif (false) {"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-PHASE-GATES-FALLBACK]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-INTEGRATION-NONCURRENT-IS-RED",
			"title": "a selected atom herdr reports as outdated or needs-repair leaves block [1] a neutral `reported` and the pane at exit 0, so a surface that has ALREADY classified those two into `profile.fail` — and whose build runner refuses them — reports them as if nothing were wrong. `no aggregate green` keeps each axis's own receipt; it never meant an axis that failed leaves at zero",
			"subject": "plugins/herdr/lib/capability-report.mjs",
			"find": ["\tif (failing.length > 0) {"],
			"replace": ["\tif (false) {"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-INTEGRATION-NONCURRENT-IS-RED]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-READER-EXPORT-IS-REQUIRED",
			"title": "the export check is dropped, so an installed reader that loads without `readCertifiedLedger` raises its TypeError inside the certification try and is filed as `activation-ledger-uncertified` — the operator is sent to inspect a ledger that is fine instead of to the install that is not, and no body was ever read",
			"subject": "plugins/herdr/lib/activation-evidence.mjs",
			"find": ["\tif (typeof reader.readCertifiedLedger !== \"function\") {"],
			"replace": ["\tif (false) {"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-READER-EXPORT-IS-REQUIRED]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		},
		{
			"claim": "HPL-LEDGER-VANISHED-IS-NAMED",
			"title": "the LATE null guard is dropped, so a ledger that passed the presence check and is gone by the time the installed reader reads it is dereferenced — the early branch cannot cover that race because it already answered while the file existed, and the pane dies with a TypeError before drawing a block",
			"subject": "plugins/herdr/lib/activation-evidence.mjs",
			"find": ["\tif (ledger === null) {"],
			"replace": ["\tif (false) {"],
			"gate": ["bash", "run.sh", "check-herdr-plugin"],
			"timeoutSeconds": 180,
			"signature": "[QK:HPL-LEDGER-VANISHED-IS-NAMED]",
			"signatureSource": "scripts/check-herdr-plugin.ts"
		}
	]
}
