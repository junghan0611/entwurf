# NEXT — `acp-on-v2` 브랜치 (ACP plugin on v2 core, B 방향)

> 부트섹터: **지금 어디 · 다음 한 걸음 · 넘으면 안 되는 선**만.
> base = `v2-only`(clean floor / 지도, **불가침 보존**). 이 브랜치에서 v2 core 위에 ACP를 다시 심는다.
> 영속 invariant(ACP=plugin 경계, 트러스트 경계)는 **AGENTS.md가 SSOT**. 이 파일 = *현 방향 + 구현 reference*(소모성).
> 흔들릴 때 앵커: botlog `20260522T092950…__…mitsein_pi.org` heading `* [2026-06-18] ACP도 데리고 간다` + 그 안의 **GLG 원본 프롬프트 3블록**(요약 금지).

# NOW (boot) — 🟢 S2g/축3 DONE+COMMITTED · 🟢 Detour B preventive steer DONE(`5e260fd`) · 🟡 Detour A root deferred(#42 candidate) · stem=PR-polish→merge · NEXT=퇴근 handoff → fresh-session live 검증/문서정리

> **현재 상태(2026-06-19 퇴근 handoff).** Repo clean before this NEXT edit. 최근 닫힌 코드 커밋(시간순): `c29e546`(A-c/B-a actionable render hints), `39a8a0e`(NEXT detour close), `d0d639a`(detour hint AMBER tighten + corpus, GPT 검수 반영), `5e260fd`(live peer intent preventive steer). `pnpm check`는 Opus 보고 기준 exit0, 방금 확인한 narrow gates: `check-acp-backend-preflight` + `check-entwurf-v2-surface` pass. **주의:** tool description/schema steer는 새로 뜬 session/backend가 읽는 예방책이다. 이미 살아 있던 Opus/ACP 세션은 예전 tool description을 들고 있을 수 있으므로, “고쳤는데 왜 또?”를 판단하려면 fresh `/gnew` 또는 새 resident로 재검증한다.
>
> **Stem:** PR-polish→merge. S2g/축3은 이미 durable: config passthrough, bundled MCP resident/RPC smoke(`937e8ae`, live 14/14), S2g-b phantom 반증. 다음 큰 줄기는 README/ROADMAP/CHANGELOG/packaged docs stale 정리 + install/doctor overlay 재현성 + release-gate MUST(LIVE=1·SKIP=0). **S2 구현을 더 밀어붙이지 않는다** unless GLG가 #42 go를 명시.
>
> **Detour B — live/meta peer intent UX:** `c29e546`의 render hint는 사후 안전망이고, `5e260fd`가 예방책이다. 양 surface(MCP + pi-native)의 `entwurf_v2` top description과 `intent` param에 못 박음: liveness=alive peer 메시지/회신/핸드오프는 `intent:fire-and-forget`(+답 필요시 `wants_reply:true`), meta-session(liveness=unsupported) 회신도 fire-and-forget→mailbox, `owned-outcome`은 dormant pi spawn-bg resume 전용이며 live/unsupported에선 reject되고 auto-convert 없음. **코드/결정자 불변.** 남은 건 fresh-session live behavior 확인뿐; 실패하면 caller/schema uptake 문제로 본다.
>
> **Detour A — old resume 첫 턴 overflow:** 아직 “해결” 아님. `c29e546`은 API Error를 actionable하게 만든 mitigation일 뿐, 근본은 #42 persisted `resumeSession(no-replay)`/delta-only 또는 window policy. 주말에 할 경우에도 먼저 설계+게이트부터: turn-scoped persisted resume 허용, turn-scoped success record persist, spawn+initialize 후 capabilities 읽고 decide, `agentCapabilities.sessionCapabilities?.resume` 사용, `loadSession`은 보류, resume 경로 setModel 생략+`models.currentModelId === record.modelId` fail-loud, `resourceNotFound`→same-child newSession fallback은 명시 notice+record invalidation/persist. Live acceptance는 cross-process codeword recall + fake backend로 “resume prompt가 delta-only” 증명.
>
> **다음 한 걸음:** 오늘은 멈춤. 다음 세션 시작 시 (1) `git status` + 이 NOW 재독 (2) fresh session에서 Detour B smoke: idle GPT peer에 handoff를 시켜 첫 호출이 fire-and-forget인지 확인 (3) GLG가 #42 go를 주면 A 설계/게이트부터, 아니면 PR-polish docs/release-gate로 복귀.

# (이전) NOW — S2 전체 DONE + S2f progress-visibility DONE(GPT GREEN, amber 2 반영), 다음 = PR-polish

> **stem**: **S2 전체 완료**(2026-06-19) — S2e-1(carrier/augment 라이브) + S2e-2(RGG) 둘 다 GPT(`…32a6c8`) 적대 검수 GREEN. S2e-1: `smoke-acp-carrier-augment-live`(augment가 wire 타고 모델 도달 행동증명 + 빈 carrier billing clean=핀1 라이브) GPT 4포인트 PASS. S2e-2: `smoke-acp-rgg-live`(기존 RGG runner를 ACP target으로=코드 0줄 lane, **deterministic 30/0 PASS** — garden discipline이 ACP provider서도; T3=모델 자율 entwurf_self는 ACP child `mcpServers:[]`라 N/A=경계, 버그 아님). `pnpm check` exit0. 다음 한 걸음 = **PR-polish**(README "v2 core + ACP plugin" 재작성 + ROADMAP + CHANGELOG + live gate 정렬). 1b-2c(persisted resume/load)=deferred 숨은덩어리. ⛔ **핀1**(라이브 확인): 캐리어 작게/순수 유지, 리치는 first-user prepend.
> **검수 trail (전부 GPT `…32a6c8` GREEN)**: **1c** = 6포인트 + (C)(D) PASS, amber 2 반영(상세 = §RECENT + `b484fea`). **S2e-1** = 4포인트(augment 정직성·billing canary·carrier sweep·pack) PASS. **S2e-2** = T3 FAIL은 ACP 경계(child `mcpServers:[]`→entwurf_self surface 없음, 메일박스 부재와 같은 결)로 GPT 합의 N/A; deterministic 30/0이 release-blocking MUST. 처리 = (a) VERIFY/NEXT 문서 + (b) `smoke-acp-rgg-live` wrapper(deterministic only), shared runner 분기(c) 회피.
> **PR-polish 진입 reference**: S2 전체(S0~S2e) DONE — ACP가 v2 core 위 plugin으로 raw turn+overlay+event mapping+session reuse+carrier/augment+RGG까지 라이브 증명됨. PR-polish = README 재작성("v2 core + ACP plugin" 프레임, 옛 "ACP 제거" 폐기 — §재triage 참조) + ROADMAP.md("ACP plugin on v2 core" lane) + CHANGELOG + install-meta-bridge 정식경로 유지 + live gate 정렬(release-gate에 ACP live smoke 5개 + `smoke-acp-rgg-live` 편입 검토). §다음 SSOT 정렬 참조. **S2 코드는 동결, 문서/release-gate만.** continuity: **Opus만 re-mint, GPT `…32a6c8` 유지(전 컷 검수 anchor).**

## RECENT
- **[2026-06-19] ✅ Detour B preventive steer DONE (`5e260fd`) — live/meta peer intent bug 정리.** GLG/GPT 지적: render hint는 사후약방문이고, caller가 처음부터 `fire-and-forget`을 고르게 해야 한다. 그래서 `entwurf_v2` 두 surface(MCP `mcp/pi-tools-bridge/src/index.ts` + pi-native `pi-extensions/entwurf-control.ts`)의 top description과 `intent` param에 명시: **liveness=alive peer(live pi/socket-citizen) 메시지·회신·핸드오프 = `intent:fire-and-forget`**, 답 필요하면 `wants_reply:true`(owned-outcome 아님), **meta-session(liveness=unsupported) 회신도 fire-and-forget→mailbox**, **`owned-outcome`은 dormant pi spawn-bg resume 전용**이고 live/unsupported에선 reject되며 auto-convert 금지. `scripts/check-entwurf-v2-surface.ts`가 양 surface 문구를 pin(42 checks), 기존 decider/matrix가 owned×live reject/fire×live send를 pin. **주의:** 기존 live session은 old tool description을 들고 있을 수 있음 → fresh `/gnew`/새 resident로 behavior 재검증 후 “현장 해결” 판단. B-a render hint(`c29e546`)는 fallback으로 유지.
- **[2026-06-19] 🟡 Detour A root DEFERRED — overflow는 A-c hint만, #42 persisted resume는 주말 candidate.** 관찰: 오래된 pi-shell-acp session resume 첫 턴이 full-transcript replay로 backend context window를 넘으며 API Error. `c29e546`의 `actionableAcpBackendHint`는 `prompt is too long`/`context window`류를 잡아 turn/full-replay 원인과 임시 회피(새·짧은 세션), 근본 후보(persisted resume/window policy)를 알려주는 **정직화**일 뿐 root fix가 아니다. #42로 갈 때의 설계 blocker: ① 현재 `decideBootstrap` resume slot은 process-scoped만 reachable → Detour A를 고치려면 **turn-scoped persisted resume** 필요 ② turn-scoped new/resume 성공 후 record persist 필요 ③ initialize capabilities가 필요하므로 decision order 재구조화(spawn+initialize 후 persisted resume/new 판단) ④ caps path는 `agentCapabilities.sessionCapabilities?.resume`(+ top-level `loadSession`, 단 load는 보류) ⑤ `loadSession`은 history replay 위험으로 #42 범위 밖 ⑥ resume 경로는 setModel 생략 + `models.currentModelId === record.modelId` fail-loud(캐노니컬/alias 주의) ⑦ `resourceNotFound` fallback은 same child newSession + notice + old record invalidation + 새 record persist. **검증 먼저:** deterministic `session-store` matrix + fake backend로 resumeSession 호출/mcpServers+_meta 재전달/delta-only prompt/no setModel/resourceNotFound fallback 증명, LIVE cross-process codeword recall. 급하게 “진짜 해결” 금지.
- **[2026-06-19] 🔴 베이스라인 발견 — ACP opus에 MCP/skill 미전달 = operator config passthrough 미구현 (GPT Blocker 1.3과 동일 뿌리).** GLG 베이스라인 테스트: `pi-shell-acp/claude-opus-4-8`로 띄운 Claude가 tool 4개(Bash/Edit/Read/Write)+MCP none+skill none만 봄. **진단(코드 확정)**: `backend.ts:626-640`이 operator의 `piShellAcpProvider.{mcpServers,skillPlugins,tools,settingSources,appendSystemPrompt}`를 **안 읽고 하드코드** — `tools:DEFAULT_CLAUDE_TOOLS(4개)`·`settingSources:[]`·`strictMcpConfig:false`·`skillPlugins:[]`·`newSession({mcpServers:[]})`. 동적은 `model.id`+`engraving`(env-override path)뿐. `tool-surface.ts:147`에 skillPlugins→`plugins` 처리 로직은 **있으나** backend가 빈 배열 전달이라 死. = 0.11.0 oracle G(MCP server normalize·validate)가 acp-on-v2에 **미이식**. README는 이 config들이 먹는다 주장 → 거짓(=GPT Blocker 1.3). **결정 필요(GLG)**: (A) 정직화=README/Settings/Custom-skills를 "현재 minimal 4-tool, explicit mcpServers/skillPlugins/tools passthrough는 후속 lane"으로 수정(PR-polish 완성, S2 동결 유지) vs (B) 구현=backend가 operator config를 newSession에 전달(사용자가 원하는 "전달" 실현, S2 코드 동결 깸=새 lane S2g). ⚠️ 설계 정합: README Concept primer "Explicit piShellAcpProvider.mcpServers only(ambient 아님)" = explicit passthrough는 원래 지원 방향 → (B)가 설계 정합이나 lane 분리. **GLG가 A/B 결정 후 진행.**
- **[2026-06-19] GPT 추가 파악 — 다음 Opus 첫 작업은 (B) S2g config passthrough로 잡는 게 맞음.** 근거: 이 repo의 `.pi/settings.json`와 global `~/.pi/agent/settings.json` 둘 다 `piShellAcpProvider.mcpServers.pi-tools-bridge`가 이미 있음 → install wiring은 살아있고, **provider backend만 안 읽는 상태**. 구현 reference는 `v0.11.0:index.ts` `readSettingsFile/loadProviderSettings` + `v0.11.0:acp-bridge.ts` `normalizeMcpServers/enrichMcpServersWithEnvelope/buildClaudeSessionMeta/newSession`. **S2g 최소 설계**: ① 새 `lib/acp/config.ts`(또는 backend-local)로 global+project `.pi/settings.json` 읽기, project override(단 mcpServers map은 global+project merge), bad config는 throw(`McpServerConfigError`/settingsConfigError); ② `mcpServers` normalize(stdio/http/sse, env/headers 검증, deterministic sort/hash), `skillPlugins` absolute dir + `.claude-plugin/plugin.json` fail-loud; ③ defaults: `settingSources=[]`, `strictMcpConfig=true`, `tools=DEFAULT_CLAUDE_TOOLS`, `permissionAllow=DEFAULT_CLAUDE_PERMISSION_ALLOW`, `disallowedTools=DEFAULT_CLAUDE_DISALLOWED_TOOLS`, `skillPlugins=[]`; nonempty skillPlugins면 **자동으로 `Skill` + `Skill(*)` 추가**(0.11.0 실측: 없으면 skill listing empty); ④ `newSession({cwd, mcpServers: enrich(pi-tools-bridge env PI_SESSION_ID/PI_AGENT_ID), _meta: buildClaudeSessionMeta(resolved, engraving)})`; ⑤ `loadEngraving({mcpServerNames: resolved.mcpServers.map(name)})` + first-user augment도 같은 names; ⑥ `bridgeConfigSignature`를 **mcpServers/settingSources/tools/skillPlugins/permissionAllow/disallowedTools/strictMcpConfig + engraving**까지 포함하도록 확장(아니면 config 변경 후 live child reuse가 stale); ⑦ `appendSystemPrompt:true`는 이번 핀1과 충돌하므로 context.systemPrompt를 carrier에 넣지 말 것 — false/absent만 허용하거나 true는 fail-loud로 막기. **게이트**: deterministic config loader + backend fake capture에서 mcpServers/Skill/plugins/strict-mcp-config/permissionAllow가 wire에 들어가는지, config change가 reuse를 끊는지; LIVE smoke는 scratch `.pi/settings.json`에 test MCP/skill plugin 넣고 모델이 MCP/skill visibility를 정직하게 보고하거나 호출 가능한지 확인.
- **[2026-06-19] PR-polish 1차 구현 + GPT(`…100902`) 적대 재검수 = 아직 GREEN 아님 (Blocker 3개). GPT 90%+ 퇴근.** 4파일 변경(run.sh/README/ROADMAP/CHANGELOG, **미커밋**, `pnpm check` exit0, bash -n OK, check-pack 174). **GREEN 받은 것**: D1 CHANGELOG(단일 Unreleased "v2 core + ACP plugin on v2 substrate", removed-ACP headline 폐기, "legacy fat bridge replaced by acp-provider+lib/acp", durable만) + D4 release-gate(ACP LIVE 7개 MUST, `run_live_step` honest-SKIP, CUT needs LIVE=1/SKIP=0). **Blocker 3개(미해결, 다음 Opus)**: ① **README false current claims** — install intro "Claude or Codex backend"(Claude-only), OpenClaw "repo carries plugins/*"(파일 없음), Settings/Custom-skills overclaim(skillPlugins/tools/settingSources 실제 안 먹음 ← 위 베이스라인과 동일 뿌리), Context-carriers 잘못된 경로(root `prompts/engraving.md`↔실제 `lib/acp/prompts/engraving.md`, `pi-context-augment.ts`→`lib/acp/augment.ts`), Compaction-policy stale(CODEX_HOME/smoke-compaction-policy 없음), What-repo-owns가 persisted continuity overclaim(실제 in-memory+record write만, persisted resume/load OFF). ② **README-linked packaged docs stale**(check-pack 포함 public tarball: `docs/setup-clean-host.md`·`demo/README.md`·`demo/demo.sh`·`demo/compaction-policy-smoke/` — smoke-all/codex/gemini/entwurf_send/@zed codex-acp 죽은 명령) → 업데이트 or 링크 제거/historical 표시. ③ **ROADMAP lower half**(핵심아키텍처/frozen-decisions/backlog)가 top과 모순("기존 entwurf/_resume/_send 유지", "0.12 cutover" 미래형, entwurf.ts source guard, #8 entwurf_send UX) → historical 표시 or 수정. **Amber(run.sh)**: setup_all print "ACP backends dropped"(false, 복귀), bottom warnings "removed pi-shell-acp provider"/"broken on v2-only", usage "v1 verbs(xt-tool-surface/sentinel) gone"(xt/sentinel은 **게이트**지 verb 아님). GPT 마지막 말: "release-gate architecture는 good, 남은 risk는 public docs가 현재 install/config/backend를 거짓 서술." **continuity: GPT `…100902` 퇴근(이 review가 마지막 trail) → 다음 GPT mint 시 이 §RECENT가 sync 기준. Opus만 살아서 Blocker 닫고 재검수.**
- **[2026-06-19] ✅ S2f progress-visibility DONE (GPT `…100902` 적대 리뷰 GREEN blocker 0 + amber 2 반영)** (thinkpad 이관 후 GLG 새 invariant — ACP turn 진행 "무조건 보임", 옵션 0. 원문: "Claude Code였으면 그냥 보였을 것, pi라서(openclaw 지원 시 지저분) 숨긴 것 되돌림. 옵션은 적을수록"). 침묵구간(overlay→spawn→init→newSession→setModel→첫토큰)에 stream lifecycle notice: **new** `[acp: preparing claude session]`→`[acp: session ready model=…]`→`[acp: sending prompt]` / **reuse** `[acp: reusing live session]`→`[acp: sending prompt]`. **GPT 설계 보정 핵심 = display-only marker**: `pushNotice`가 `output.content`에 들어가 assistant transcript content가 되므로(내 "출력측만" 주장 순진), marker 없이는 ① `new` rebuild full transcript에 `[acp:…]` replay ② `contextMessageSignatures`에 섞여 reuse compat 흔듦. ⇒ `textSignature="pi-shell-acp:lifecycle-notice-v1"` 박고 양측 필터: event-mapper `pushAcpLifecycleNotice`(showToolNotifications **무시**=항상 보임, tool notice와 분리) + marker SSOT / context.ts `textFromAssistantContent` filter(replay 차단) / session-store.ts `messageContentSignature` **block 자체 제외**(빈 entry 아님 — `|`-join shift 방지 → signature invariant). marker는 소비측(context/session-store)에서 **로컬 mirror const**: strip-types 게이트가 `.ts` 소스 직접 로드라 cross-sibling **value** import(`./event-mapper.js`) resolve 불가 — lib은 type-only 공유 규율(backend만 컴파일 경로 예외). 게이트 `check-acp-session-reuse` **Section F**(notice 순서 + captured ACP prompt에 `[acp:` 없음 + buildAcpPrompt('new') marker drop + signature invariance + mirror 일치 직접 assert) + `smoke-acp-provider-live`(3줄 live 가시 + **L3 marker persistence**: known `--session-id`/`--session-dir`로 JSONL에 `textSignature` 보존 실측=reload 후 filter 작동 전제 닫음). raw child stderr는 그대로 에러시만(노이즈 분리=지저분 안 함). **green**: `pnpm check` exit0, provider-live PASS(3줄 화면 가시 + L3 round-trip), reuse-live PASS(turn2 reusing만+codeword recall 유지=marker filter 라이브 reuse 안 깸). **GPT amber 2 반영**: ① marker persistence L3 assert(provider-live JSONL) ② `prompt sent`→`sending prompt`(prompt() sync-reject 가능성 — evidence-first 문구). **금지선**: notice 텍스트에 pid/cwd/acpSessionId/sessionKey 노출 안 함. 변경 6파일. **durable=커밋 메시지. 다음 = PR-polish.**
- **[2026-06-19] DETOUR — test/release-gate taxonomy anchor issue 생성 (#41)**: `testing/release-gate surface를 증명 체계 기준으로 재정리` → https://github.com/junghan0611/pi-shell-acp/issues/41 (실무 방향 코멘트 포함). **이건 detour**: 지금 당장 다 뒤집지 말고, 노트북 이관/PR-polish에서 VERIFY·BASELINE·release-gate를 다시 쓰기 위한 앵커를 박아둔 것. 해야 할 일 = 이 이슈를 기준으로 검증 자산을 deterministic / MUST live / BEHAVIOR / utility / 퇴출후보로 재분류하고, release-gate를 capability matrix 기준으로 다시 읽기. ⛔ **속도 우선**: S2 이후 남은 PR-polish/merge lane을 이 detour가 늦추면 안 됨.
- **[2026-06-19] ✅ S2e-2 RGG(ACP-provider resident garden guard) DONE + GPT GREEN** (GPT `…32a6c8` 합의: 기존 runner를 ACP target으로 = 코드 0줄 lane). `smoke-acp-rgg-live` wrapper(run.sh) = `PI_SHELL_ACP_LIVE_TARGET=pi-shell-acp/claude-sonnet-4-6 SMOKE_RGG_POSITIVE=0` → 공유 `smoke-resident-garden-guard.sh` 호출(`PI_SHELL_ACP_RGG_TARGET` override). **deterministic 30/0 PASS**: NEGATIVE(uuid resident turn 전 폭발)·REPLACEMENT(/new·/clone 취소)·RESUME-INTO-UUID(pre-cancel)·GNEW(garden birth+socket rebound+no uuid leak+control tag). garden discipline이 ACP provider서도 완벽(S1 citizenship 위 별 invariant). **T3(POSITIVE, 모델 자율 entwurf_self)=N/A by ACP 경계**: child `mcpServers:[]`라 entwurf_self surface 없음(메일박스 부재와 같은 결, 버그 아님, BEHAVIOR advisory). 처리 GPT 합의 = (a) VERIFY/NEXT 문서 + (b) wrapper deterministic only, (c) runner 분기 회피. stale 주석("v2-only ACP removed") 정정. **durable=커밋**. **S2 전체(S0~S2e) 완료 → 다음 PR-polish.**
- **[2026-06-19] ✅ S2e-1 carrier/augment 라이브 DONE + GPT GREEN** (GPT `…32a6c8` 설계 합의 → 구현 → 적대 검수 GREEN, blocker 0, 코드수정 0). 신규 `scripts/smoke-acp-carrier-augment-live.ts`(LIVE-gated, OUT of pnpm check) + run.sh 등록(`smoke_acp_carrier_augment_live` 함수 + case). scratch cwd AGENTS.md에 unique secret(프롬프트 외부) → 빈 carrier 1턴서 모델 secret 회수(augment가 wire 타고 모델 도달 행동증명) + 빈 carrier billing clean(exit0·400 canary 부재=핀1 라이브) + optional `SMOKE_ACP_CARRIER_PRESENT=1` tiny carrier도 clean+marker present. GPT 4포인트 PASS(augment 정직성=행동증명·billing canary=좁은 패턴·carrier sweep 불필요·pack 기존 live smoke와 일관). Read 우회 가능성은 주석에 정직하게 명시(행동증명 한계 인정, 과한 안전장치 회피). **durable=커밋**. **follow-up**: 없음 → S2e-2(RGG).
- **[2026-06-19] ✅ S2d-1c billing carrier + first-user augment DONE(구현 + GPT 검수 GREEN)** (GPT `…32a6c8` 설계 GREEN 합의대로 · `pnpm check` exit0 · **GPT 구현 적대 리뷰는 다음 Opus** — GLG가 위임). 신규 `lib/acp/engraving.ts`(carrier 로더 — env override `PI_SHELL_ACP_ENGRAVING_PATH`+path-keyed cache+`{{backend}}`/`{{mcp_servers}}` 정렬 보간, trim후 empty→null, **순수**: clock/random/env 없음) + `lib/acp/prompts/engraving.md`(**빈 기본**→carrier 부재 by default, operator opt-in) + `lib/acp/augment.ts`(`buildPiContextAugment`=bridge identity+task stance+pi base+tool surface+`~/AGENTS.md`+cwd/AGENTS+date day-gran+cwd, 50KB truncate, `homeDir` seam / `removeCwdAgentsSectionFromAugment` 0.11.0 verbatim / `promptCarriesEntwurfCwdContext`=`<project-context path="cwd/AGENTS.md">` includes / `prependNewPromptAugment`=wire-level prepend). `backend.ts`: carrier 1회 렌더 → `bridgeConfigSignature(appendSystemPrompt)` + `buildClaudeSessionMeta(_,engraving??undefined)` **동일 주입**(carrier 바뀌면 sig 바뀜, 순수→매턴 rebuild 없음, null→""/undefined→1b-2b byte-identical) · augment는 `new` 경로 prompt에만 wire-prepend(pi context 미접촉→ctxSigs 무관, once-only=path 분기 self-clearing, pending-slot 불필요). 게이트 `check-acp-carrier-augment`(IN check, 8섹션). **durable 디테일=커밋 메시지**. **분기점 4개 GPT 확정**: ① 빈 carrier 기본 ② same-string→sig/meta ③ cwd-AGENTS de-dup 포함 ④ `new` 1회 prepend, pending-slot 없음. **GPT 적대 리뷰 GREEN(2026-06-19, blocker 0)**: 확정 6포인트 + (C)(D) PASS, amber 2 반영(engraving.ts 주석 정밀화 / gate marker `ENTWURF_PROJECT_CONTEXT_OPEN_TAG` 상수 import) · `pnpm check` exit0. **다음 = S2e**.
- **[2026-06-18] ✅ S2d-1b-2b in-memory session reuse DONE** (GPT `…32a6c8` round-2 GREEN · `pnpm check` exit0). `backend.ts` 대수술: `streamShellAcp`→`streamAcpTurn(model,ctx,opts,deps)` 위임(deps seam로 결정론 prompt-capture). in-memory `bridgeSessions` Map + `retainedChildren` Set + `inFlightKeys` Set. **reuse=delta-only**(`buildAcpPrompt(ctx,"reuse")`) / new=full transcript. **mutable `activePromptHandler`** 라우팅(connection 콜백 위임, `applyAcpSessionUpdate` single-site). lifecycle: process-scoped만 retain+`unrefRetainedChild`(unref≠destroy, exit-pin 방지), turn-scoped는 teardown(S2c exit0 보존). 동시성: busy(retained) + first-turn race(`inFlightKeys` spawn 전 claim) 둘 다 fail-loud. incompatible→new는 old child close(orphan 방지), model-lock throw는 no-close+survive. record write만(persisted resume/load=OFF, =1b-2c). session-store sha256 hardening 3개(configSig/recordPath/ctxSigs, toolResult에 toolName·isError). 게이트 `check-acp-session-reuse`(IN check, 6섹션 capture+routing+busy+race+orphan+source) + `smoke-acp-session-reuse-live`(LIVE 2턴 codeword recall). **durable 디테일=커밋 메시지**. **GPT 검수 trail**: round-1에서 blocker 2개(first-turn race, incompat orphan)+amber 1개(retained stdio exit-pin) 잡힘→round-2서 다 닫고 GREEN. **follow-up**: SIGINT/TERM best-effort cleanup(GPT 동의, exit-pin은 unref로 닫힘 → 1c/1d).
- **[2026-06-18] ✅ entwurf_v2 `owned×socket-only` 거짓 `bad-target` 버그 고침** (detour, 1b-2b 前 surgical). 살아있는 socket-only pi peer에 owned-outcome → 거짓 bad-target(=없는 타깃)이던 것을, decider 2b short-circuit 제거 + 새 reject reason `socket-only-no-resume-authority`로 정직화. owned×live→`owned-live-no-autosend`, owned×dormant→`socket-only-no-resume-authority`, fire×live=불변. GPT(`…32a6c8`) 5점 합의. `pnpm check` green(contract 242/decider/production PASS). **durable 디테일=커밋 메시지**. 변경: `entwurf-v2-{decider,contract}.ts` + `check-entwurf-v2-{decider,contract,production}.ts` + `smoke-entwurf-v2-matrix-live.ts`. ⚠️ **라이브 재검증 보류**: 이 세션 MCP는 편집 前 기동 → 재기동 후 owned×live-socket→owned-live-no-autosend 확인(GLG). caller 규칙: live socket-only peer엔 `fire-and-forget`(+wants_reply), owned는 record-backed dormant resume 전용.

## 목표 (한 줄)

**v2 core + ACP overlay-ingress plugin.** v1은 끝. ACP는 데리고 간다 —
0.11.0을 *기계적으로 port하지 않고*, **구조는 새로 / 행동은 0.11.0(behavior oracle)에서 이식**.
**rename 안 함**(`pi-shell-acp` 이름 유지 — ACP가 살아있으니 이름이 정합).

# NOW

- **Current**: **S0 + S1 DONE (2026-06-18).**
  - **S0 loader/fence**(커밋 `4afa58e`): `pi-extensions/acp-provider.ts`(thin entry) + `lib/acp/{models,backend-stub}.ts`, `pi.extensions` 맨 앞 등록. 큐레이트 Claude(sonnet-4-6 + opus-4-8 anchor), no-auth sentinel, **fail-loud streamSimple stub**(backend·native fallback·bash/env 우회 전부 없음). 게이트: `check-auth-boundary`(retarget) + `check-acp-provider-surface`(temp `tsc` emit → 컴파일 entry fake-pi 실행 캡처). pack stale(#4) RED→GREEN. `pnpm check`/`check-pack-install` green + 라이브 `pi --list-models`.
  - **S1 socket-citizen**(이 세션): **라이브 증명 완료** — `pi --entwurf-control --provider pi-shell-acp --model claude-opus-4-8 --mode rpc` resident가 production `entwurf_peers`에 `liveness=alive model=pi-shell-acp/claude-opus-4-8 idle=yes`로 떠서 native Codex 형제 옆 1급 시민. get_info(cwd/model/idle) 응답. **QM1**(model-lock가 ACP 모델 런치를 안 되돌림 — in-session switch에만 발화) + **QM2**(fail-loud stub은 턴에서만, 런치는 turn-free라 안 죽음) 둘 다 라이브 차단. 아티팩트: `scripts/smoke-acp-socket-citizen-live.ts` + `./run.sh smoke-acp-socket-citizen-live`(LIVE-gated, OUT of pnpm check, 10 checks PASS — GPT 검수 후 peers-visible(`scanSocketProbes`) + 소켓 잔여-0 hygiene check 추가). **턴 0회 — citizenship만 증명, backend turn은 S2.** **pushed**(`fe20436` + GPT 후속 보정).
- **S2-scout DONE + GPT PASS (2026-06-18)**: 3핀 정찰 read-only 완료 → GPT(`…be0e35`) 검수 PASS. 확정값 = §S2-scout 핀(하드제약).
- **S2a-1 DONE (2026-06-18)**: dep pin(`@agentclientprotocol/sdk@0.22.1` + `claude-agent-acp@0.39.0`) + **peer-resolution pin `@anthropic-ai/sdk@0.100.1`**(GPT 옵션A 승인 — claude-agent-sdk 0.3.156의 `>=0.93.0` peer 충족용, source 미사용). surface gate `check-acp-sdk-surface`(**5 layer**: exact pins / lock peer-resolution / **runtime peer-resolution probe**(claude-agent-sdk context에서 anthropic-sdk→0.100.1 실제 resolve) / wire-SDK value-export / no-client-use[side-effect·export-from·dynamic 확대]) + `pnpm check` 등록. **GPT 상세검토 PASS + 후속 보강 반영**. typecheck/게이트 green. 커밋 `cc6aee2`(+보강 커밋). GPT anchor rotate: `…be0e35`→`…441eab`.
- **S2a-2 DONE (2026-06-18)**: raw ACP 1턴 라이브 증명. `smoke-acp-raw-turn-live`(LIVE-gated, OUT of pnpm check): claude-agent-acp를 **package bin**에서 spawn → `ndJsonStream`+`ClientSideConnection`으로 initialize(protocolVersion=1)→newSession(scratch cwd, mcpServers:[], **`_meta` 부재**)→`unstable_setSessionModel(claude-sonnet-4-6)`→prompt("say OK") → 라이브 **"OK"** + **raw NDJSON 25755 bytes** 캡처. permission/file-op 0. PATH fallback=acceptance FAIL(debug override만). **provider/overlay/streamSimple 0줄** — backend-stub fail-loud 유지. GPT 보강3: `ndJsonStream`을 export gate에 추가. **S2a(dep+raw) 완결.**
- **S2b DONE (2026-06-18) + GPT PASS**: Claude overlay materializer + tool-surface pure preflight + overlay-aware raw ACP live turn. Proves child spawn env `CLAUDE_CONFIG_DIR=<overlay>`, overlay `hooks:{}` and tool-narrowed `_meta.claudeCode.options` with `_meta.systemPrompt` absent. Provider `streamSimple` wiring/event mapping remains S2c.
  - **Cut1 (deterministic, IN pnpm check)**: `pi-extensions/lib/acp/overlay.ts`(0.11.0 `ensureClaudeConfigOverlay` Claude-only 이식 — projects/sessions=overlay-private real dirs(symlink 아님) / passthrough whitelist 9개=`.credentials.json,cache,debug,session-env,shell-snapshots,skills,stats-cache.json,statsig,telemetry` / plugins·agents·settings.local.json·CLAUDE.md 제외 / binary-owned `.claude.json,backups,settings.json` stale-symlink만 정리·real 보존 / `overlaySettingsJson`={defaultMode:default,autoMemory:false,hooks:{}} / `claudeLaunchEnvDefaults`→`CLAUDE_CONFIG_DIR`) + `tool-surface.ts`(`DEFAULT_CLAUDE_TOOLS/PERMISSION_ALLOW/DISALLOWED_TOOLS` + `assertExcludeToolsHonored` **pure preflight**(wire read 아님, effective config=SSOT) + `buildClaudeSessionMeta(params,systemPrompt?)` — 인자 없으면 `_meta.systemPrompt` 부재(핀1 carrier guard)). 게이트 `check-acp-overlay`(temp realDir/overlayDir 주입 + stale symlink(non-whitelist·binary-owned·source-gone whitelist) 정리·binary-owned real 보존·idempotent·`CLAUDE_CONFIG_DIR` 검증) + `check-acp-tool-surface`(matrix: claude narrows/native always-exposes/extension-free/plural + meta shape + no-carrier). 둘 다 `pnpm check` 등록.
  - **Cut2 (LIVE-gated, OUT of pnpm check)**: `scripts/smoke-acp-overlay-live.ts`(S2a smoke 동결 — 별도 acceptance). realDir=operator ~/.claude(live creds)·overlayDir=mkdtemp·scratch cwd 유지·spawn env `CLAUDE_CONFIG_DIR=overlayDir`. **라이브 PASS**: child `/proc/<pid>/environ` `CLAUDE_CONFIG_DIR=<overlay>` ✓ / overlay `hooks:{}` 확인 / tool-narrowed `_meta`(systemPrompt 부재 단언) / setSessionModel(sonnet) / prompt→"OK" + raw NDJSON 11843 bytes / permission·fileop 0. **메일박스 부재는 전역 meta-store diff 안 함**(동시 세션 flaky — GPT Q1); 정직한 claim=overlay가 hooks:{} 공급. GPT 보강2(binary-owned stale symlink 제거 + source-gone whitelist 제거 게이트 케이스) 반영.
- **S2c DONE (2026-06-18) + GPT PASS (anchor `73b44d`)**: event mapping + `streamSimple` 실 backend 교체. provider path 처음 열림, fail-loud stub 제거.
  - **S2c-1 (커밋 `2e926e1`)**: `lib/acp/event-mapper.ts`(Claude-only 축약 — agent_message_chunk→text / agent_thought_chunk→thinking / tool_call·update→**text notice([tool:start/running/done/failed/cancelled])** 구조화 toolcall 0 / usage_update→coarse totalTokens·cost.total) + `lib/acp/context.ts`(`contextToAcpPrompt`=transcript passthrough, systemPrompt/thinking/구조화tool 제외, image marker non-silent, 마지막 user 포함). gate `check-acp-event-mapper`(IN pnpm check).
  - **S2c-2 (커밋 `82495dc`, docs `297fa50`)**: `lib/acp/backend.ts` `streamShellAcp`(동기 stream + queueMicrotask: overlay→spawn(detached)→initialize(caps:{})→newSession(S2b `_meta`, **systemPrompt 부재**)→setSessionModel→prompt(contextToAcpPrompt)→mapper→done+end). **runtime preflight**(spawn 前 `assertExcludeToolsHonored`, diverge→stream error). error/abort=throw 아니라 stream error event. permission approve-all+notice / readTextFile=real fs / writeTextFile=fail-loud. teardown 비차단(stdio destroy+unref+detached group-kill) + **withTimeout clearTimeout**(dangling 600s 타이머가 pi hang 유발했음, 수정). stub 제거 + 게이트 `check-acp-provider-surface` 실backend 인식(name+source-shape, invoke 안 함) + `check-acp-backend-preflight`(tsc-emit, no-spawn 증명). live smoke `smoke-acp-provider-live`(실 pi provider 경로 nonce + exit0). pack required 5런타임파일.
- **Next = S2d (billing carrier + session reuse + engraving + first-user augment)**: 이제 안전하게 진입 가능(raw pipe+overlay+event mapping 다 살아있음). 핀1 billing carrier(engraving SHORT/empty=`_meta.systemPrompt`, 순수함수) + session store/signature/reuse(bootstrapPath reuse|resume|load|new) + first-user message augment(bridge identity+~/AGENTS.md+pi intro, de-dup). 상세 = §S2-scout 핀1 + oracle A~C + oracle G·H.
  - **⚠️ 핀4 prompt 범위 = bootstrapPath 종속 (GPT `73b44d` S2d 진입 전 경고 + Opus 0.11.0 실측)**: 지금 `contextToAcpPrompt`는 spawn-per-turn 전제라 **전체 transcript flatten**(`context.ts:2-7` 주석). S2d에서 session reuse를 그대로 얹으면 ACP 세션이 이미 보유한 history에 전체 transcript를 **재주입 → 중복**. ⇒ prompt builder를 분기: `new`(rebuild 포함) = 전체 transcript(현 `contextToAcpPrompt` 유지 — 새 ACP 세션은 history 0이라 transcript가 유일한 history 운반; 0.11.0보다 안전한 의도적 개선) / `reuse·resume·load` = **마지막 assistant 이후 first-user delta만**(0.11.0 `index.ts:732 extractPromptBlocks` 실측 — `reverse().find()` 아니라 trailing group의 first user여서 SessionStart hook-msg 회피). **augment(bridge identity+AGENTS+pi intro)는 `new` 첫 prompt에만 prepend 후 clear**(0.11.0 line 2511·3394 — reuse 턴 cache 보존 + 빌링 캐리어 작게). first-user augment de-dup은 그 위에서만 작동.
    - **S2d 구현 게이트 (GPT `73b44d` PASS 조건 — 결정론적 builder 게이트로 고정)**: ① persisted `resume·load`는 `contextMessageSignatures` **prefix-compat 통과 시에만** delta-only 안전 — prefix 불일치/compaction/edited history면 persisted path 버리고 `new`로 fallback(full transcript). delta-only가 틀려지는 유일 케이스를 막는 핵심. ② augment는 **compat signature에 미포함**(아니면 매턴 drift), pending slot으로 `new` 첫 prompt만 consume/clear — `reuse·resume·load` 재-prepend 금지. ③ `context.systemPrompt`→`_meta.systemPrompt` 승격 금지(carrier=short/pure only, rich=first-user augment). ④ `new` full transcript의 토큰/컨텍스트 길이 비용은 S2c가 이미 감수한 history-preservation 비용 — **billing-400 classifier 축 아님**(user prompt payload), 후속 max-window/summary 정책 사안이지 delta-only로 history 잃을 이유 아님.
    - **S2d-1a builder cut DONE (GPT `73b44d` PASS · `pnpm check` green)**: 순수 builder `context.ts`의 `buildAcpPrompt(ctx, bootstrapPath)`/`latestUserDelta(ctx)`(0.11.0 `extractPromptBlocks` 이식)/`AcpBootstrapPath` 타입 + deterministic gate `check-acp-prompt-builder`(IN pnpm check — new=full transcript / reuse·resume·load=delta / hook-msg 회피 / no-assistant·trailing-assistant·empty 엣지 / unknown-path→throw). **builder hardening(GPT 검수)**: `buildAcpPrompt`=switch+default `throw`(unknown bootstrapPath=fail-loud, silent delta-only=history-loss fail-open 차단) / `latestUserDelta`=empty 판정만 trim·본문 verbatim 보존(delta=실 user prompt, 0.11.0 near-raw 정합). **backend wiring은 의도적 보류** — `streamShellAcp`는 S2c 그대로 `contextToAcpPrompt`(=항상 new) 호출, gate만 builder를 검증(실경로 무변). builder를 backend에 꽂는 건 **S2d-1b**(session store가 실제 bootstrapPath 공급 시 prefix-compat 게이트 ③와 함께). **S2d-1b 게이트 요구(GPT)**: backend가 `buildAcpPrompt(context, bridgeSession.bootstrapPath)`를 **실제로 호출**하는 source/behavior check 포함(wiring 정직성). 커밋 분리(GLG): feat=`context.ts`+`scripts/check-acp-prompt-builder.ts`+`run.sh`+`package.json` / docs=`NEXT`.
  - **S2d-1b 설계 굳음 (GPT `73b44d` PASS)**: **bootstrapPath(history source: new|reuse|resume|load)와 lifecyclePolicy(`process-scoped` vs `turn-scoped`) 분리** — 두 축 독립. in-memory child/connection reuse = **long-lived process 한정**(resident/RPC). **`pi -p` one-shot = 항상 new+teardown**(S2c 동작 유지 — stdio handle이 exit를 pin하므로 exit-hook 정리는 hang 회귀 위험; 첫 cut에서 persisted resume/load for one-shot도 금지, 미래 turn-scoped persisted는 별도 follow-up). **carrier-first 분리**: S2d-1b는 carrier 부재(`appendSystemPrompt` empty) → signature=model당 상수 → reuse lifecycle만 검증, engraving carrier(`_meta.systemPrompt` SHORT 순수)+first-user augment=**S2d-1c**. **sub-cut**: 1b-1(deterministic: `bridgeConfigSignature`/`contextMessageSignatures`/`isCompatible`(sig+prefix-compat)/`decideBootstrap`/record store, 순수+temp-dir gate) → 1b-2(live: in-memory session map + reuse wiring + long-lived 2턴 reuse smoke + one-shot S2c exit0 유지 확인). 1b-1 게이트: ① modelId guard를 순수 decision에도(record에 modelId; existing alive mismatch→fail-loud throw) ② prefix-compat 불일치/compaction/edited→new+full transcript ③ carrier drift→incompatible(existing close/persisted delete) ④ augment는 signature 절대 미포함(1c pending one-shot).
    - **S2d-1b-1 DONE (GPT `73b44d` PASS · `pnpm check` green)**: 신규 `pi-extensions/lib/acp/session-store.ts`(`bridgeConfigSignature`/`contextMessageSignatures`/`hasPrefix`/`isCompatible`/`decideBootstrap`/`SessionRecord` store I/O — backend 미접촉, child/connection 0) + gate `check-acp-session-store`(IN pnpm check, 매트릭스 8케이스+roundtrip+corrupt삭제). `decideBootstrap`: model-lock throw(live alive mismatch)→turn-scoped 항상 new→process reuse/resume(cap)/load(cap)/new. record에 modelId 추가(0.11.0 미보유, cross-process reopen mismatch를 incompat로). strip-types 호환 위해 parameter-property 회피. **S2d-1b-2 진입 전 GPT 권장**: sessionKey는 `options.sessionId` 우선, 긴 cwd fallback은 길이 리스크라 **hash로 바꾸는 시점=1b-2 이전이 최적**(현재 `encodeURIComponent`는 안전하나 길이 미해결).
    - **S2d-1b-2a DONE (GPT `73b44d` PASS · `pnpm check` green)**: `resolveLifecyclePolicy(argv=process.argv)` + `ENTWURF_CONTROL_FLAG`를 `session-store.ts`에 추가(backend 미접촉) + gate `check-acp-session-store` 섹션6(5케이스). **후보 A**: exact `--entwurf-control` 토큰만 `process-scoped`, `-p`/interactive/look-alike 전부 `turn-scoped`(S2c hang-safe). 키는 resident flag never `-p`(resident가 `-p` 동반 가능 — B 함정 gate로 고정). argv 인자화로 순수 테스트. 미래 명시 pi lifecycle hint는 argv보다 우선 확장 여지.
    - **S2d-1b-2b DONE (2026-06-18, GPT `…32a6c8` round-2 GREEN · `pnpm check` exit0)** — 아래 확정 설계(`73b44d`/`c617cb`) 그대로 구현. process-scoped에서만 in-memory `bridgeSessions` Map에 child/connection 보관 + reuse 턴이 기존 connection에 `buildAcpPrompt(ctx,"reuse")` delta 전송 + 턴 성공 후 record write(ctxSigs 갱신) + cleanup(`process.once('exit')` 전역1회 sync group kill, retained child unref로 exit-pin 방지). **persisted resume/load는 안 탐(=1b-2c)**: record write만, capability 안 넘김. one-shot exit0=turn-scoped teardown 유지. GPT round-1 추가 보정 반영: first-turn race(`inFlightKeys`), incompatible→new old child close, retained `unrefRetainedChild`. 게이트 `check-acp-session-reuse`(prompt-capture로 delta-only 직접 증명) + `smoke-acp-session-reuse-live`(LIVE 2턴 codeword recall). **SIGINT·TERM best-effort cleanup은 follow-up(1c/1d, GPT 동의 — exit-pin은 unref로 닫힘).**
      - **1b-2b 확정 설계 (GPT `73b44d` 마지막 정렬 — rotate 전 review-trail)**: ① **persisted resume/load는 1b-2c로 분리** — 1b-2b는 **in-memory reuse + new만**(record write는 하되 persisted read/use는 끔 / capabilities=false→new). ② **CRITICAL — mutable handler 라우팅**: 보관 `ClientSideConnection`의 callback이 첫 턴 `state`를 클로저로 잡으면 안 됨(2턴째 notification이 끝난 첫 stream으로 새거나 유실). `BridgeSession.activePromptHandler`(mutable)를 두고 callback은 `session.activePromptHandler?.(event)`로 위임 — 매 prompt 직전 현재 streamState로 set, finally clear. `requestPermission`도 같은 라우터. **없으면 idle connection 살아도 event mapping 틀림 → 1b-2b 필수 게이트.** ③ reuse는 child stdin/stdout/stderr **destroy 금지**(`teardownChild`는 turn-scoped/incompatible-close 때만). 동시 prompt는 `busy` 플래그로 **fail-loud**(첫 cut). ④ record write=턴 **성공 후** params.ctxSigs(이번 call 시작 context) 저장, **실패/abort 시 미갱신**. ⑤ cleanup registry=process-scoped retained child만; incompatible/dead→new면 map 제거+old child best-effort close, **model-mismatch throw 시 close 금지**. ⑥ sessionKey=`options.sessionId`/`PI_SESSION_ID` 우선(cwd fallback은 prefix가 대부분 막지만 완전 identity는 sessionId). 필수 게이트: buildAcpPrompt source check + **callback이 mutable handler 위임 source/behavior check** + long-lived 2턴 reuse smoke + one-shot exit0 유지.
      - **1b-2b 넓은 검수 보강 (GPT `c617cb` GREEN — 1b-2b 첫 커밋에 선행)**: **hardening 3개(1b-1 코드 sha256화 — record/sig에 raw 안 남게)**: ① `bridgeConfigSignature`=`sha256(JSON.stringify(...))` digest ② `sessionRecordPath`=`sha256(sessionKey).json`(현 `encodeURIComponent` 대체 — 길이/안전) ③ `contextMessageSignatures` 각 항목=`sha256(role:content)`(raw text 저장 금지) + **toolResult sig에 toolName/isError 포함**. prefix-compat는 해시 배열로 그대로 작동. **게이트 prompt-capture**: source check(buildAcpPrompt 호출)만으론 부족 — fake connection/capture로 **2턴째 prompt에 1턴 token 없음(delta-only)** 직접 증명("기억했다"만으론 full transcript 재주입 여부 미증명). **추가 안전**: abort/error→`activePromptHandler` clear + map 제거 + child close(불확실 connection reuse 금지) / child exit between turns→`exit` 이벤트로 alive=false+map delete(다음 턴 new, 죽은 child를 compatible 착각 금지) / cleanup registry=`process.once('exit')` **전역 1회 + retained child set 순회**(session마다 once 쌓으면 EventEmitter leak) / stderr drain=retained lifetime 내내 bounded. **금지선(얇음 유지)**: compaction policy / multi-backend abstraction / awareness·fact publication / MCP normalize generalized router 지금 끌어오지 말 것(Claude stable 후 별도 lane). **session-store=substrate fact 아니라 provider-internal backend cache** — peers/facts에 ACP child session id/record 노출 금지. **큰 그림(usable 전망)**: 1b-2b 후=비용/latency usable 근접 / 1c(carrier+augment) 후=존재감 usable(GLG 실사용) / S2e+RGG+README/ROADMAP/CHANGELOG 후=PR merge. 숨은 덩어리=persisted resume/load(1b-2c)+first-user augment de-dup(1c), 둘 다 범위 보임.
- **잠긴 방향 (굳음)**:
  - ACP backend = overlay 격리 **소켓-시민**, 메일박스 **설계상 부재**(overlay `settings.json` `hooks:{}` → meta-bridge hook 없음 → 메일박스 없음; *같은* minimal 설정이 기본도구만 = 가벼움. 한 결정이 둘을 만듦).
  - **차별 없음 = 시민 층위**(entwurf_peers 가시 / garden id 주소화 / entwurf_v2 도달 / 응답), *레일 동일성 아님*. 메일박스 부재 = right-sizing(라이브라 "나중에 닿기" 불필요).
  - **ACP plugin = pi 세션의 model/provider로 들어감.** socket-citizenship은 host `--entwurf-control` pi 세션이 **공급**(plugin이 새로 안 만듦). plugin 책임 = provider/model 등록 → backend spawn → overlay → streamSimple. **새 socket/peers/citizen protocol 금지(=과설계 실패모드).** Cortex=`SNOWFLAKE_HOME` overlay = 같은 패턴.
  - **스코프**: Claude 먼저(테스트 리그 + 크레딧 헤지) / Codex 스킵(native direct-inject로 이미 시민) / Cortex·기업용=payoff(벤더락=native 불가, hvkiefer PR #40 재오픈까지 완전검증 대기) / 메이저툴=native.
- **설계 질문 6개 (정하지 말고 들고만 있기)**:
  1. overlay 재현 — `settings.json` minimal(`hooks:{}`+autoMemory off) + auth/skills/cache symlink passthrough + sessions/projects/settings 로컬 격리.
  2. v1 **model-side** 배선 이식성 — **채굴결과(06-18): 새 소켓 안 만듦.** `socket-discovery`는 model-agnostic이라 host `--entwurf-control` 세션이 시민권을 공급(소켓 파일명=gardenId, RPC로 cwd/model/idle enrich). → v1의 *model/provider* 배선만 v2 기판으로 이식; 소켓/peers 레이어는 상속.
  3. repo 구조 — **#15 청사진**: `acp/backends/claude.ts` · `acp/overlays/*` · `acp/session-store.ts` · `acp/model-lock.ts` · `acp/compaction-policy.ts` (+ 얇은 facade, **#38 보정**: 중심 아닌 plugin 하나). #15가 extraction으로 못 닿은 shape를 *fresh build*로. **단 물리위치는 #6과 연동 미결**: top-level `acp/*` vs `pi-extensions/lib/acp/*`.
  4. Cortex 검증 한계 — 로컬 완전검증 불가(계정 없음), Claude만 지금 가능.
  5. fact 표면(#39) — **socket/liveness/addressability(exists / live / socket·control path / replyable·addressable)는 host `--entwurf-control` 세션이 socket-discovery로 공급**(plugin이 만드는 게 아님 — Q1 결론). ACP plugin 자신의 fact = **backend health / turn evidence**뿐. *그 이상(memory·planner) 금지.*
  6. **ACP entry/fence/pack 위치 (#15 벽 — 반만 해결, GPT 보정)** — pi extension은 **jiti로 TS 로드**(Node strip-types 아님). → 질문은 "strip-types로 로드되나"가 아니라 **"provider entry를 어디 두고, entry+내부모듈이 어떤 typecheck/runtime/pack fence를 타나"**. v2 crossing pattern은 *있음*(lib/* = root exclude → `scripts/tsconfig` allowImportingTsExtensions+noEmit; emit-root는 non-literal dynamic import로 닿음), 단 `scripts/tsconfig` include=`../pi-extensions/lib/**/*.ts`라 **top-level `acp/*`는 자동 진입 안 됨**. **택1(GPT lean=①)**: ① `pi-extensions/acp-provider.ts` entry(`package.json` `pi.extensions` 추가) + 내부 `pi-extensions/lib/acp/*`(기존 fence 탑승) / ② top-level `index.ts` 부활(v0.11 유사 — **중심성 회귀 위험**) + 새 tsconfig 경계. → **S0에서 고정**.

# S2-scout 핀 — 확정값 (read-only 정찰 + GPT PASS 2026-06-18, 하드제약)

> S2 코드 진입 전 디버깅 지옥 방지용. 이 3핀은 S2a~S2d 내내 불변.
> **틀: 이건 재구현이지 port가 아니다.** 0.11.0은 동작하지만 v1 + 무거움 → behavior oracle만 이식, 구조는 새로. **pi는 4번째 하네스(Claude Code·Codex·Antigravity·pi)일 뿐, ACP는 그 pi에 붙는 plugin 하나**(중심/2번째 하네스 금지).

**핀1 billing carrier (oracle A, 최고위험)** — 0.11.0 `engraving.ts` 실측:
- `_meta.systemPrompt`가 SDK-default 모양서 material하게 멀어지면 → 구독(OAuth 정액제) billing이 metered 분류 → 잔액없는 구독자 HTTP400(캐리어 커지는 순간).
- ⇒ 캐리어(engraving)=SHORT/empty. 리치컨텍스트(AGENTS/pi base/bridge identity/tool surface)는 `pi-context-augment.ts`가 **첫 user message에 prepend**. 캐리어 rendered=(template-on-disk, backend, mcpServerNames) **순수함수**(clock/random/env 금지 → 아니면 `bridgeConfigSignature` drift → 매턴 rebuild). Claude-only면 캐리어 1슬롯 붕괴.

**핀2 ACP SDK surface** — 0.11.0 deps + registry(2026-06-18):
- pin = `@agentclientprotocol/sdk@0.22.1` + `@agentclientprotocol/claude-agent-acp@0.39.0` (둘 다 0.11.0값). latest는 0.26.0/0.47.0(claude-agent-acp 8 minor drift) — **upstream 업그레이드는 raw turn+overlay+event mapping green 뒤 별도 lane**.
- 사일런트 rename 없음(`@agentclientprotocol/*` 유지, pin registry 생존). spawn: `require.resolve("@agentclientprotocol/claude-agent-acp/package.json")`→bin→fallback `PATH:claude-agent-acp`.
- **`@anthropic-ai/sdk` 직접 dep 금지** — 0.11.0 소스에 직접 import/`new Anthropic()` 0건(API client 아님). 실제는 `@anthropic-ai/claude-agent-sdk`(+platform-native) cli.js/native binary를 `require.resolve` probe→로컬 인증 claude spawn. runtime resolve 실패시 조사 대상 = `@anthropic-ai/claude-agent-sdk`(SDK API client 아님). 우선 claude-agent-acp가 transitive로 끌고 오는 걸로 충분한지 게이트 확인.
- `@zed-industries/codex-acp` scope-out(native가 Codex 도달).

**핀3 local Claude ACP auth** — 실측:
- `claude 2.1.181` PATH ✅ / `~/.claude/.credentials.json` ✅. overlay `~/.pi/agent/claude-config-overlay/`: credentials/skills/cache/debug/session-env **symlink passthrough** + projects/sessions **로컬 격리**.
- overlay `settings.json` = `{"permissions":{"defaultMode":"default"},"autoMemoryEnabled":false,"hooks":{}}` → `hooks:{}`=메일박스 부재 by design 실측 + autoMemory off. **claude-agent-acp 바이너리 미설치**(S2a가 dep 설치). auth=claude 바이너리가 자체 process FS에서 읽음(repo가 안 옮김 → invariant 정합).

**GPT 명문 하드제약 3문장 (영속)**:
1. First S2 implementation pins ACP deps to 0.11.0 versions; upstream upgrade is a later lane.
2. ~~No direct `@anthropic-ai/sdk` dependency unless source imports it~~ **REVISED (2026-06-18, S2a-1 실측 + GPT 옵션A 승인)**: `@anthropic-ai/sdk` direct dep is allowed ONLY as an exact peer-resolution pin (`0.100.1`) for `@anthropic-ai/claude-agent-sdk@0.3.156`'s `>=0.93.0` peer; source-level import / API-client / credential use remains forbidden. 근거: drop하면 stale `0.91.1`로 resolve → peer unmet → raw-turn-time break. 0.11.0 lockfile이 동일 구조 증명. 게이트 `check-acp-sdk-surface`가 강제(pin 일치 + lock peer-resolution + wire-SDK export + no source import/`new Anthropic()`).
3. S2a proves bytes/one raw turn only; no overlay, no provider streamSimple replacement, no session reuse/signature, no rich carrier/augment.

**S2a 경계 (GPT 확정)**: dep pin + SDK surface gate + stdio JSON-RPC ACP raw 1턴까지만. LIVE-gated · scratch cwd · `_meta.systemPrompt` 안 키우는 minimal payload(S2a에서 리치 컨텍스트/AGENTS prepend/engraving 실험 금지).

**Continuity**: GPT anchor `20260618T080922-be0e35`가 이 S2-scout PASS를 끝으로 퇴근(rotate). 위 §검수 trail이 review-state. 다음 GPT는 새로 mint — 이 §S2-scout 핀 + 본 NEXT가 sync 기준.

# 구현 순서 (S0 → S1 → S2 — GPT)

> **첫 slice는 backend가 아니다.** 0.11.0은 behavior oracle이지 architecture oracle 아님. socket/peers/entwurf_v2/v2 core 손대기 금지, v1 부활 금지.

- ~~**S0 loader/fence slice (첫 coding)**: ACP provider extension entry 위치 결정 + `pi.extensions` 연결 + typecheck fence + `check-pack`/`check-pack-install` required 정렬 + `pi --list-models`에 curated Claude anchor.~~ **DONE 2026-06-18** (위 NOW 참조). GPT lean ① 채택(`pi-extensions/acp-provider.ts` entry + `lib/acp/*` 내부, `.js` import로 root fence 자동 탑승 — 새 tsconfig 없음). fail-loud stub + 게이트 2개 + pack stale 정리까지.
- ~~**S1 socket-citizen**~~ **DONE (2026-06-18)**: ACP-model `--entwurf-control` 세션이 `entwurf_peers`에 1급 시민으로 뜨고 `get_info`에 답함 — 라이브 증명 + `smoke-acp-socket-citizen-live`(8 checks). QM1/QM2 라이브 차단. **메일박스 부재·overlay 활성 증명은 S2로**(S1은 backend turn 전이라 불가 — GPT).
- **S2 backend turn**: 그 세션에서 실제 ACP Claude **1턴 성공** + overlay 활성(`settings.json` `hooks:{}` = 메일박스 부재 by design)·도구축소·이벤트매핑(rawOutput=array) 안 깨짐. ← ACP backend 건강 + overlay 증명(S1만으론 미증명).
- 각 단계 `pnpm check`/typecheck green. **주의: `pnpm check` green ≠ publish 가능** — 아래 §따라온 이슈 pack-install 참조.
- **Blocker**: none. commit/push/tag/merge = GLG.
- **Read**: 이 파일 + **AGENTS.md**(영속 경계) + botlog 앵커 + 이슈 **#38**(ACP=plugin)/**#39**(awareness=read-only fact)/**#15**(구조청사진+트러스트경계) + (착수 시) 0.11.0 ACP 코드.

# 홉 계획 + Continuity (계획이지 예언 아님 — Opus/GPT 합의 2026-06-18)

> ⚠️ 이건 **끊는 흐름**을 위한 계획이지 고정 예언이 아니다. 숫자는 감각치(절대화 금지), S2 내부 설계 세부·새 파일명은 여기서 확정하지 않는다(그건 oracle A~H + 착수 시 코드가 SSOT).

**3층 골격**: S1=구조 확정층 / S2=실제 backend 구현층 / PR-polish=공개·merge 준비층.

- **S1 (작게 — 1 Opus + 1 GPT)**: backend turn 없이 "ACP-model `--entwurf-control` 세션이 `entwurf_peers`+`get_info`에 잡히나"만. socket-discovery가 model-agnostic이라 코드-라이트일 가능성 큼.
  - 사전 물음표 2개(진입 첫 동작으로 차단): ① `model-lock.ts`가 ACP 모델 선택을 되돌리나 ② `pi --entwurf-control --model pi-shell-acp/…` 런치가 stub 때문에 *시작* 시 죽나(런치+peers+get_info는 턴 없음 → 안 죽어야 정상).
- ~~**S2-scout (S2a 앞단에 흡수 — 코드 전 핀)**~~ **DONE + GPT PASS (2026-06-18)**: 3핀 실측 + 확정값 = §S2-scout 핀. (billing carrier oracle A / ACP SDK surface / local Claude auth 전부 read-only 정찰 완료.)
- **S2 컷 (≈4~5 Opus + 2~3 GPT)** — 순서가 안전장치 (**현재: S2 전체[S0~S2e] DONE, 전부 GPT GREEN, 다음 = PR-polish**):
  - ~~**S2a** dep 핀 + stdio JSON-RPC ACP client + **raw 1턴**~~ ✅ **DONE (2026-06-18, Opus 2nd)**: dep pin+peer-pin+5-layer gate (`cc6aee2`/`f3ef6b8`) + raw 1턴 라이브 sonnet "OK" (`e0ddb2b`/`6fc8b52`). GPT 최종 PASS.
  - ~~**S2b** overlay + 도구축소 + `assertExcludeToolsHonored`~~ ✅ **DONE (2026-06-18, Opus 3rd)**: overlay materializer + tool-surface pure preflight + overlay-aware live turn. 게이트 2개 + live smoke. GPT PASS.
  - ~~**S2c** 이벤트매핑 + `streamSimple` 실 backend 교체(stub 제거)~~ ✅ **DONE (2026-06-18, Opus 3rd)**: S2c-1 event-mapper+context+gate / S2c-2 streamShellAcp+runtime preflight+stub제거+live provider smoke. dangling-timer pi-hang 버그 잡음. GPT(`73b44d`) PASS. **S2c 코드/docs pushed**(S2c-1 `2e926e1` / S2c-2 `82495dc` / S2c-DONE docs `297fa50`). 그 위 **S2d-1a builder cut(GPT PASS·green)** + 핀4 NEXT 보정은 worktree에 미push — GLG commit/push 시 `origin/acp-on-v2 == acp-on-v2`·clean (GLG가 docs를 최종 커밋으로 stamping).
  - **S2d** session store/signature/reuse + billing carrier(engraving) + first-user augment. **진행: 1a(builder)·1b-1(store)·1b-2a(lifecycle)·1b-2b(in-memory reuse)·1c(carrier+augment) DONE, 1c GPT 적대 리뷰 GREEN(amber 2 반영). 다음=S2e. 1b-2c(persisted resume/load)=deferred 숨은덩어리.**
  - ~~**S2e** live smoke + 게이트 + RGG~~ ✅ **DONE (2026-06-19, 둘 다 GPT GREEN)**: S2e-1=`smoke-acp-carrier-augment-live`(augment wire 도달 + 빈 carrier billing clean=핀1 라이브) / S2e-2=`smoke-acp-rgg-live`(deterministic 30/0, garden discipline ACP provider서도; T3=ACP child `mcpServers:[]` 경계로 N/A). **S2 전체 완료 → PR-polish.**
- **PR-polish (1~2 Opus + 1 GPT)**: README/ROADMAP/CHANGELOG/live gate.
- **감각치(절대화 금지)**: usable+merge까지 대략 6~8 Opus 왕복 / 4~5 GPT 리뷰.

**Continuity rule (꼭 지킬 것)**:
> Do not rotate Opus and GPT at the same time. The GPT/Codex session is the review/continuity anchor; Opus can be re-minted from NEXT + AGENTS + botlog + the latest GPT review. If GPT must rotate, first write a short review-state into NEXT or botlog.

- 즉 **어느 컷에서도 Opus·GPT 둘 다 동시에 새로 가지 않는다.** 기본: GPT 세션(현 `20260618T211518-32a6c8` — `be0e35`→`441eab`→`73b44d`→`c617cb`→`32a6c8`로 이어옴; `73b44d`가 1b-2b 확정 설계, `c617cb`가 넓은 검수 보강, **`32a6c8`가 1b-2b 구현 round-1/2 적대 검수→GREEN**)을 살려두고 Opus만 re-mint. (S0/S1/S2a~S2c가 그 키트로 깨끗이 re-mint된 게 증거 — Opus가 NEXT+AGENTS+GPT 리뷰만으로 re-mint돼 매 컷 완주.) 깨지는 경우 = 둘 다 새로 갈 때 → GPT 닫기 전 review-trail을 NEXT/botlog에 남길 것.

# 0.11.0 behavior oracle — 구현 reference (소모성)

> ⚠️ 다른 문서로 새지 말 것 — 여기가 **유일 사본**. 구현 끝나면 코드/CHANGELOG로 승격 후 이 섹션 삭제.
> 원칙: **behavior는 배우고, architecture/중심성은 안 가져온다.**

- **A. 빌링 캐리어 규칙 (가장 비싼 hard-won)**: `_meta.systemPrompt`(Claude 캐리어)가 SDK-default 모양에서 멀어지면 Anthropic 구독 빌링이 **metered로 분류 → 구독자 HTTP 400**(operator 실측). ⇒ 캐리어(`engraving`)는 작게, 리치 컨텍스트(bridge 정체성+`~/AGENTS.md`+pi intro+tool surface)는 **첫 user message에 prepend**(긴 user 메시지와 구조 동일 → 빌링 무관). 50KB 상한, 날짜 day 단위. *모르고 재구현하면 400 재발.*
- **B. 정체성 캐리어 비대칭**: Claude=`_meta.systemPrompt` 문자열치환 / Codex=`-c developer_instructions`(spawn, pinned, 바꾸려면 respawn) / Gemini=overlay `system.md`(`GEMINI_SYSTEM_MD`). **Claude-only면 1슬롯으로 붕괴.**
- **C. 안정성 서명 → 세션 재사용**: `bridgeConfigSignature=hash(systemPromptAppend+mcpServers)`. 캐리어가 턴마다 drift하면 **매 턴 세션 rebuild**(비쌈) ⇒ 캐리어 입력은 **순수함수**(clock/random/env-time 금지, 날짜 day 단위). bootstrapPath=reuse|resume|load|new, record=`~/.pi/agent/cache/pi-shell-acp/sessions`, `isSessionCompatible`가 캐리어 검사. piAgentId는 서명 *이후* spawn 시 주입(stale 방지). **prompt 범위 실측(`index.ts:732 extractPromptBlocks`)**: 매 턴 **마지막 assistant 이후 first-user delta만** 전송(stateful ACP 세션이 history 보유 → reuse가 기본). S2c는 정반대(spawn-per-turn→전체 transcript). ⇒ S2d reuse 도입 = prompt 범위 분기 의무(§Next 핀4). augment 1회성: `index.ts:2511`(new 첫 prompt만, 이후 clear) + `acp-bridge.ts:3394 sendPrompt`(project-context de-dup).
- **D. overlay = 설정격리 + 도구축소 + 캐리어 물질화 (백엔드별)**: Claude=`claude-config-overlay`(settings.json `hooks:{}`=메일박스 부재, auth/skills symlink, 기본도구만=가벼움) / Codex=`CODEX_HOME`(memories gate, `web_search=disabled`) / Gemini=`admin.toml` deny-all admin-policy.
- **E. 모델 강제 + 도구 정직성**: 커레이트 모델만(전체 pi-ai 레지스트리 아님), `unstable_setSessionModel`로 강제(=set_model 아날로그). `assertExcludeToolsHonored`=선언(pi)≠실제(backend)면 **fail-fast**(모델에게 거짓말 금지). 확장도구(entwurf*)는 pi-side라 자유 제외.
- **F. 이벤트 매핑(3방언) + 권한 자동응답**: ACP `session_notification`→pi stream(message_chunk→text / thought→thinking / tool_call→notice / usage→토큰·비용). 3백엔드가 tool 결과 다르게 노출(Claude rawOutput=array / Codex=CallToolResult객체 / Gemini=없음→content[]). entwurf_send→`[entwurf sent →]` 박스(3단 args 복구), `sanitizeNoticeFragment`로 한 줄 notice 보호. 권한 자동응답(YOLO). **Claude-only면 방언 1종.**
- **G. 기타 이식 대상**: stdio NDJSON JSON-RPC reader / MCP server normalize·validate / `sendPrompt` project-context **de-dup**(entwurf-spawn 시 AGENTS.md 중복 방지) / `assertLegacyCompactionKnobUnset`(compaction off by design).
- **H. provider no-auth sentinel (구현 때 놓치기 쉬움 — GPT)**: `registerProvider("pi-shell-acp", …)` 재등록 시 "credentials 제공/재판매/우회 아님"을 *등록 형태*로 지키는 장치. 놓치면 `ANTHROPIC_API_KEY`류 오해/오작동 재발. **S0에서 구현됨**: `pi-extensions/lib/acp/models.ts`의 `PI_SHELL_ACP_NO_AUTH_SENTINEL="pi-shell-acp-no-auth"`(lowercase+hyphen → ENV ref 아님). 게이트 `check-auth-boundary`는 **신규 `acp-provider.ts` + `lib/acp/*` retarget**(legacy-ENV ALLCAPS apiKey 리터럴 금지 + sentinel 존재) — **AGENTS §Operating boundaries(trust)의 코드-레벨 짝**. (옛 `index.ts`/`acp-bridge.ts` 기준 아님.)
- **핵심 발견**: 3544줄 fat의 큰 덩어리 = **3백엔드 방언 화해**. Claude-only면 **캐리어 1슬롯 · overlay 1종 · 이벤트 방언 1종**으로 붕괴 ⇒ *재구현이 port보다 얇다*(코드로 확인됨).

# 0.11.0 읽기 전 규율 (ACP 중심 회귀 방지)
1. 0.11.0 = **behavior oracle**이지 **architecture oracle 아님**. 2. `acp-bridge.ts` 동작은 배우되 **중심성은 이식 안 함**. 3. ACP는 v2 core *바깥*에서 socket citizen 공급. 4. awareness는 read-only fact로만. (상세·영속 경계: **AGENTS.md §ACP Plugin Boundary** + §Operating Boundaries)

# 따라온 이슈 — v2-only 상속 (substrate, ACP와 무관하게 살아있음 → 그대로 따라감)

- **URGENT README/install doc debt (oracle 2026-06-17)**: checkout/pull/branch-switch 후 `pnpm install`; meta-session canonical = `./run.sh install-meta-bridge`; 검증 `./run.sh doctor-meta-bridge` + `claude mcp get pi-tools-bridge`(중립 cwd `/tmp`도 USER scope Connected); 소스 갱신 후 `pnpm install && ./run.sh install-meta-bridge` 재실행. 수동 `~/.mcp.json`/project MCP는 plain external/debug용으로 격하.
- ~~**pack-install gate stale (GPT, publish ≠ `pnpm check`)**~~: **RESOLVED S0 (2026-06-18).** `check_pack_install` tar_required에서 옛 ACP 6파일(`index.ts`/`acp-bridge.ts`/`event-mapper.ts`/`engraving.ts`/`pi-context-augment.ts`/`pi-extensions/entwurf.ts`) 제거 → 신규 `acp-provider.ts`+`lib/acp/*` 추가. `check-pack-install` 직전 RED였음(삭제파일 required) → 이제 GREEN. **주의 유지: `pnpm check`는 `check-pack`까지만 → publish 전 반드시 `check-pack-install` 별도 실행.**
- **meta-store hygiene (GLG 숙고, 미결)**:
  - *재현성*: thinkpad만 v1 정리됨, oracle 등은 v1/orphan 누적 잔존. cleanup verb(`prune --apply` / TTL 자동 / `migrate-v1` 일괄) vs 수동 janitor+문서화. 주의: prune 1.0.0 정책 = **listing-only**(보수적, store는 native→garden lookup 권위) → `--apply`는 정책 변경이라 가볍게 추가 말 것.
  - *store 성장 근원*: 매 Claude 세션 1 record mint + 자동 prune 없음 → 부풀음("지저분" 근원). retention/aging 정책 정할지 결정.
  - *v1 reader 제거 타이밍*: dual-read v1 경로(`parseMetaRecordV1`/`normalizeMetaIdentity` v1 분기) 제거는 **전역 마이그레이션 완료에 게이트** — thinkpad 하나로 당기지 말 것.
  - *model=null (cosmetic)*: 새 record `model` null(Claude hook envelope에 model 없음). 동작 무관, 별도 후속.

# 재triage 필요 — B가 옛 전제를 뒤집음 (⚠️ 그대로 쓰지 말 것)

> v2-only NEXT의 "Phase B 잔여"는 **rename + ACP 제거** 전제였다. B는 둘 다 뒤집는다. 옛 메모를 그대로 실행하면 드리프트다.

- **model-lock (반전 주의)**: 옛 메모 = "ACP 제거로 막을 provider 없어 vestigial". **B에선 ACP가 돌아오므로 `native↔pi-shell-acp provider` 전환이 다시 의미를 가질 수 있음.** *삭제로 드리프트 금지* — ACP 재구현 설계와 함께 재정의.
- **provider 하드코드 / resolve-acp-bridge**: rename 안 하므로 `getRegistryRouting`의 `provider:"pi-shell-acp"`는 *유지*. `scripts/resolve-acp-bridge.ts`(v2-only에서 orphan)는 ACP 복귀로 *되살아날 수 있음* — 새 구조에서 재설계.
- **README 재작성**: 여전히 필요하나 프레임 변경 — "ACP 제거"가 아니라 "**v2 core + ACP plugin**". install-meta-bridge 정식 경로 + 실패 해석은 유지.
- **dead export / sentinel / session-messaging**: `runEntwurfSync`/`runEntwurfResumeSync`(호출처 0), sentinel(LEGACY), session-messaging — 옛 메모는 "rename 직전 절삭". B에선 새 구조 정리 시 재판단. 지금 LEGACY fail-loud 보존.

# 다음 SSOT 정렬

> **AGENTS.md 보정 = 이번에 완료**(ACP=plugin 경계 + 트러스트 invariant 반영, v2-only 절삭 프레임 제거). 남은 것:
- **ROADMAP.md**: "ACP plugin on v2 core" lane 추가 — ACP 설계 굳은 뒤.
- **README 재작성**: "v2 core + ACP plugin" 프레임 — 구현 후(위 재triage 참조).

# 넘으면 안 되는 선
- **다음 = PR-polish — S0~S2e 전부 DONE, 전부 GPT GREEN(S2 완료).** S0~S2e-1 커밋푸시 완료(`…4c6f359`); S2e-2(wrapper + 주석 + VERIFY 문서)는 이 커밋. §S2-scout 핀이 하드제약. **PR-polish 진입점**: §NOW boot "PR-polish 진입 reference" = README("v2 core + ACP plugin" 프레임, 옛 "ACP 제거" 폐기) + ROADMAP + CHANGELOG + live gate 정렬. **S2 코드 동결, 문서/release-gate만.** **continuity: GPT `…32a6c8` 살아있음(1b-2b/1c 검수 anchor) — Opus만 re-mint.** 1b-2c(persisted resume/load)=deferred. **⛔ 핀1 위반 주의**: 캐리어가 SDK-default서 material하게 멀어지면 구독 billing→metered→HTTP400. 캐리어=작게/순수, 리치컨텍스트는 first-user prepend(1c가 이렇게 구현됨 — 깨지 말 것). **⛔ 핀4 위반 주의**: reuse는 bootstrapPath별 분기됨(new=transcript / reuse=delta), augment는 `new` 1회만(wire-level, pi context 미접촉). socket/peers/`entwurf_v2`/v2 core 손대기 금지. **v1 `entwurf`/`entwurf_send`/`entwurf_resume` 절대 부활 금지.**
- **드리프트 금지**: 옛 칼날("ACP 제거 / rename")로 돌아가지 말 것. ACP는 *데리고 간다*. 흔들리면 botlog 앵커 + 원본 프롬프트 3블록 재독.
- **경계 금지**: ACP가 다시 *중심 하네스*가 되지 말 것 — ACP는 v2 core 바깥 plugin(상세: AGENTS.md §ACP Plugin Boundary). plugin을 memory/planner/orchestrator/second harness/mailbox citizen으로 키우지 말 것.
- **`v2-only` 브랜치 불가침**: base/지도라 보존. 작업은 이 브랜치에서만.
- `core.hooksPath`/`.git-hooks-mode` 불변. commit/push/tag/publish/merge = GLG. `--no-verify` 금지.
- **0.11.0 = behavior oracle. 기계적 port 금지** — 행동만 이식, 구조는 새로.

# 참고
- base 지도: `NEXT--v2-only.md`(v2-only 브랜치에 보존) = 무엇이 최소 생존 셋인지의 정찰 기록.
- 0.11.0 ACP 코어(되살릴 reference, v2-only에서 삭제됨): `acp-bridge.ts`(3544) / `engraving.ts`(125) / `event-mapper.ts`(720) / `pi-context-augment.ts`(183) / `index.ts`(1263) + `prompts/engraving.md` + `scripts/resolve-acp-bridge.ts`. read-only로 `git show v0.11.0:<file>`.
- overlay 실측: `~/.pi/agent/claude-config-overlay/` — auth/skills/cache symlink passthrough, sessions/projects/settings 로컬 격리, `settings.json` `hooks:{}`(메일박스 부재 by design).
